{
  "name": "state",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/state/src",
  "projectType": "library",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["tsc --noEmit -p packages/state/tsconfig.lib.json"]
      }
    },
    "build": {
      "executor": "@nrwl/rollup:rollup",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/state",
        "format": ["cjs", "esm"],
        "compiler": "babel",
        "entryFile": "packages/state/src/index.ts",
        "tsConfig": "packages/state/tsconfig.lib.json",
        "project": "packages/state/package.json",
        "assets": [
          {
            "glob": "packages/state/README.md",
            "input": ".",
            "output": "."
          }
        ]
      },
      "dependsOn": ["typecheck"]
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs state {args.ver} {args.tag}"
      },
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "packages/state/src/**/*.ts",
          "packages/state/tests/**/*.ts"
        ]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/state/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "semantic-release": {
      "executor": "@theunderscorer/nx-semantic-release:semantic-release",
      "options": {
        "buildTarget": "state:build",
        "outputPath": "dist/packages/state",
        "github": true,
        "changelog": true,
        "npm": true,
        "tagFormat": "state-v${VERSION}"
      }
    }
  },
  "tags": []
}
